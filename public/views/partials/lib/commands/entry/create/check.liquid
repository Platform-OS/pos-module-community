{% liquid
  assign c = '{ "errors": {}, "valid": true }' | parse_json

  function c = 'modules/core/lib/validations/presence', c: c, object: object, field_name: 'creator'
  function c = 'modules/core/lib/validations/presence', c: c, object: object, field_name: 'entry_type'

  # TODO Let's validate that the entry type is valid here.

  hash_assign object['valid'] = c.valid
  hash_assign object['errors'] = c.errors

  return object
%}
